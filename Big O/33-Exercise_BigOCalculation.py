def fun_challenge(input):
    a = 10
    a = 50 + 3

    for i in range(len(input)):
        def another_function():
            return
        stranger = True
        a += 1
    return a


"""
Answer: the function above has an O(n) complexity since our number of operations increases according to our
number of elements. The higher the number of elements, the higher the number of operations.
"""
